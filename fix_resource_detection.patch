--- a/backend/app/llm/graph/domains/resource.py
+++ b/backend/app/llm/graph/domains/resource.py
@@ -30,11 +30,12 @@ class ResourceAgent(BaseDomainAgent):
         stripped = _strip_order_by(prior_sql)
 
         # Fixed detection logic: distinguish by unique columns instead of EMPID casing
-        # Detect which resource query was prior: benched uses "employeeid" (lowercase),
-        # active uses "EMPID" (uppercase alias)
-        if "employeeid" in [c.lower() for c in prior_columns] and "EMPID" not in prior_columns:
-            # benched_resources pattern — join on employeeid (lowercase col name)
+        # Both queries return "EMPID" (without brackets), so distinguish by unique columns
+        # active_resources has "Designation", benched_resources has "TechCategoryName"
+        has_designation = "designation" in [c.lower() for c in prior_columns]
+        has_tech_category = "techcategoryname" in [c.lower() for c in prior_columns]
+        
+        if has_tech_category and not has_designation:
             # benched_resources pattern
             sql = (
                 f"SELECT prev.* "
