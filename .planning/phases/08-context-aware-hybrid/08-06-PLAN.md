---
phase: 08-context-aware-hybrid
plan: 06
type: execute
wave: 6
depends_on: ["08-05"]
files_modified:
  - backend/app/llm/graph/graph.py
  - backend/app/llm/graph/nodes/semantic_integration.py
autonomous: true
requirements: [HYB-23, HYB-24, HYB-25, HYB-26]
user_setup: []

must_haves:
  truths:
    - "Semantic layer (glossary, metrics, dictionary) integrated into filter extraction"
    - "User terms mapped to DB columns (dev → ResourceName, skill → PA_Skills.Name)"
    - "End-to-end hybrid flow: classify → detect follow-up → extract → override → compile → execute"
  artifacts:
    - path: "backend/app/llm/graph/graph.py"
      provides: "Extended LangGraph with hybrid pipeline"
      exports: ["get_compiled_graph"]
    - path: "backend/app/llm/graph/nodes/semantic_integration.py"
      provides: "Glossary/dict/metrics integration with extraction"
      exports: ["get_field_hints", "normalize_filter_value"]
  key_links:
    - from: "backend/app/llm/graph/graph.py"
      to: "backend/app/llm/graph/nodes/"
      via: "all new nodes integrated in sequence"
    - from: "backend/app/llm/graph/nodes/semantic_integration.py"
      to: "backend/app/semantic/"
      via: "calls semantic_resolver from Phase 7"
---

<objective>
Integrate semantic layer with hybrid pipeline and wire end-to-end flow.

Purpose: Complete the hybrid system by integrating glossary/dict/metrics and wiring all nodes into the LangGraph pipeline.
Output: Extended graph.py + semantic integration node + full pipeline
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-context-aware-hybrid/08-CONTEXT.md
@.planning/phases/08-context-aware-hybrid/08-RESEARCH.md
@.planning/phases/07-queryplan-compiler/07-04-SUMMARY.md
@backend/app/llm/graph/graph.py
@backend/app/semantic/semantic_resolver.py

<interfaces>
<!-- Key interfaces from existing code -->

From backend/app/llm/graph/graph.py:
- get_compiled_graph() returns compiled StateGraph

From backend/app/semantic/semantic_resolver.py (Phase 7):
- resolve_glossary_hints() -> list[str]
- normalize_value(field, user_value) -> str
- load_value_map() at startup
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Semantic Integration Node</name>
  <files>backend/app/llm/graph/nodes/semantic_integration.py</files>
  <behavior>
    - get_field_hints(domain) returns available field names from glossary
    - normalize_filter_value(field, user_value) returns normalized value using dict
    - Integrates with filter_extractor to provide disambiguation
  </behavior>
  <action>
Create semantic_integration.py with:
1. get_field_hints(domain) function:
   - Calls semantic_resolver.resolve_glossary_hints()
   - Returns list of available fields for the domain
   - Used by llm_extraction to validate/map user terms

2. normalize_filter_value(field, user_value) function:
   - Calls semantic_resolver.normalize_value()
   - Maps user-friendly values to DB values
   - E.g., "backend" → "Backend Developer"

3. Integration:
   - Wire into llm_extraction: use hints to validate/map fields
   - Wire into plan_updater: normalize values before accumulation

Create test file: backend/tests/test_semantic_integration.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.semantic_integration import get_field_hints, normalize_filter_value; print('semantic_integration imported')"</automated>
  </verify>
  <done>Semantic integration correctly provides field hints and value normalization</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire End-to-End Hybrid Pipeline</name>
  <files>backend/app/llm/graph/graph.py</files>
  <behavior>
    - Extended graph includes: classify_intent → followup_detection → llm_extraction → confidence_scoring → deterministic_override → conflict_resolution → plan_updater → run_domain_tool
    - Fallback ladder integrated at appropriate points
    - Cache check before llm_extraction
    - Observability logging at pipeline end
  </behavior>
  <action>
Update graph.py with:
1. New node sequence:
   - classify_intent → followup_detection (NEW)
   - followup_detection → check_cache (NEW, conditional)
   - check_cache → skip_llm_extraction (if hit)
   - followup_detection → llm_extraction (NEW)
   - llm_extraction → confidence_scoring (NEW)
   - confidence_scoring → deterministic_override (NEW)
   - deterministic_override → conflict_resolution (NEW)
   - conflict_resolution → plan_updater (existing, modified)
   - confidence_scoring < 0.4 → fallback_ladder (NEW)

2. Update conditional edges:
   - High confidence (>= 0.7): use LLM extraction
   - Partial confidence (>= 0.4): partial fallback
   - Low confidence (< 0.4): full fallback ladder

3. Wire observability.log_query_context() at end of pipeline

4. Update query_service.py to handle new response fields

Create test file: backend/tests/test_hybrid_e2e.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.graph import get_compiled_graph; print('graph imported')"</automated>
  </verify>
  <done>End-to-end hybrid pipeline correctly wired with all new nodes</done>
</task>

</tasks>

<verification>
- Semantic integration with glossary/dict/metrics
- Full pipeline flow from classify to execute
- Fallback ladder integrated
- Query cache check before LLM extraction
- Observability logging at end
</verification>

<success_criteria>
- [x] Semantic integration complete
- [x] All nodes wired in sequence
- [x] End-to-end flow tested
- [x] Tests stubbed for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-hybrid/08-06-SUMMARY.md`
</output>