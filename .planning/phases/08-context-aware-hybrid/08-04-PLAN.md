---
phase: 08-context-aware-hybrid
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - backend/app/llm/graph/nodes/fallback_ladder.py
  - backend/app/llm/graph/nodes/context_recovery.py
autonomous: true
requirements: [HYB-14, HYB-15, HYB-16, HYB-17, HYB-18, HYB-19]
user_setup: []

must_haves:
  truths:
    - "6-level fallback ladder: retry → heuristic → context recovery → partial → clarify → full LLM"
    - "Each level triggers based on confidence threshold and extraction failure"
    - "Graceful degradation: system never crashes, always returns result or asks user"
  artifacts:
    - path: "backend/app/llm/graph/nodes/fallback_ladder.py"
      provides: "6-level fallback chain implementation"
      exports: ["execute_fallback_ladder"]
    - path: "backend/app/llm/graph/nodes/context_recovery.py"
      provides: "Level 3 context recovery from query tokens"
      exports: ["recover_from_context"]
  key_links:
    - from: "backend/app/llm/graph/nodes/fallback_ladder.py"
      to: "backend/app/llm/graph/nodes/confidence_scoring.py"
      via: "confidence < 0.4 triggers ladder"
    - from: "backend/app/llm/graph/nodes/fallback_ladder.py"
      to: "backend/app/llm/graph/nodes/llm_extraction.py"
      via: "level 1 retries with stronger prompt"
    - from: "backend/app/llm/graph/nodes/fallback_ladder.py"
      to: "backend/app/llm/providers/"
      via: "level 6 uses full LLM fallback"
---

<objective>
Implement the 6-level fallback ladder for filter extraction failures.

Purpose: Ensure graceful degradation when filter extraction fails — from retry to heuristic to context recovery to partial execution to user clarification to full LLM fallback.
Output: Fallback ladder node + context recovery module
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-context-aware-hybrid/08-CONTEXT.md
@.planning/phases/08-context-aware-hybrid/08-RESEARCH.md
@backend/app/llm/graph/nodes/param_extractor.py

<interfaces>
<!-- Key interfaces from existing code -->

From backend/app/llm/graph/nodes/param_extractor.py (Phase 5):
- KNOWN_SKILLS, KNOWN_STATUS patterns for heuristic extraction

From backend/app/llm/agents/llm_fallback.py (existing):
- LLM fallback chain pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement 6-Level Fallback Ladder</name>
  <files>backend/app/llm/graph/nodes/fallback_ladder.py</files>
  <behavior>
    - execute_fallback_ladder(question, state, current_filters, failure_reason) returns filters
    - Level 1: Retry LLM (stronger prompt, stricter format)
    - Level 2: Heuristic Extraction (KNOWN_* constants)
    - Level 3: Context Recovery (infer from query tokens)
    - Level 4: Partial Execution (run with partial filters)
    - Level 5: Clarification (return ask_user prompt)
    - Level 6: Full LLM Fallback (generate SQL via LLM)
  </behavior>
  <action>
Create fallback_ladder.py with:
1. execute_fallback_ladder() async function:
   - Takes current state + filters + failure reason
   - Progresses through levels until success
   - Logs which level triggered for observability

2. Level implementations:
   - Level 1: Call extract_structured with stronger prompt
   - Level 2: Use heuristic patterns from param_extractor (KNOWN_SKILLS, KNOWN_DATES)
   - Level 3: Call context_recovery module
   - Level 4: Pass partial filters to sql_compiler (allow partial)
   - Level 5: Return structured clarification request
   - Level 6: Route to existing llm_fallback node

3. Threshold integration:
   - confidence < 0.4 starts at level 3 (skip retry)
   - JSON parse failure starts at level 2
   - Invalid fields starts at level 2

Create test file: backend/tests/test_fallback_ladder.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.fallback_ladder import execute_fallback_ladder; print('fallback_ladder imported')"</automated>
  </verify>
  <done>Fallback ladder correctly progresses through 6 levels based on failure type</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Context Recovery (Level 3)</name>
  <files>backend/app/llm/graph/nodes/context_recovery.py</files>
  <behavior>
    - recover_from_context(question, last_filters) returns recovered filters
    - Infers filters from question tokens when LLM extraction fails
    - Uses keyword matching against known patterns
  </behavior>
  <action>
Create context_recovery.py with:
1. KNOWN_PATTERNS dict:
   - Skills: python, java, .net, javascript, react, angular, etc.
   - Status: active, benched, approved, pending
   - Date patterns: this month, last week, q1 2024, etc.

2. recover_from_context() function:
   - Tokenize question
   - Match against KNOWN_PATTERNS
   - Return inferred filters if match found
   - Return empty list if no inference possible

3. Integration: Called by fallback_ladder level 3

Create test file: backend/tests/test_context_recovery.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.context_recovery import recover_from_context; print('context_recovery imported')"</automated>
  </verify>
  <done>Context recovery correctly infers filters from question tokens</done>
</task>

</tasks>

<verification>
- Fallback ladder implements all 6 levels
- Each level triggers based on appropriate failure condition
- Context recovery provides level 3 inference
- Graceful degradation: always returns result or clarification request
</verification>

<success_criteria>
- [x] 6-level fallback ladder implemented
- [x] Level 3 context recovery functional
- [x] Integration with confidence scoring
- [x] Tests stubbed for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-hybrid/08-04-SUMMARY.md`
</output>