---
phase: 08-context-aware-hybrid
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - backend/app/llm/graph/nodes/llm_extraction.py
  - backend/app/llm/graph/nodes/confidence_scoring.py
autonomous: true
requirements: [HYB-06, HYB-07, HYB-08, HYB-09]
user_setup: []

must_haves:
  truths:
    - "LLM extraction produces strict JSON with filters, sort, limit, follow_up_type"
    - "Confidence scoring computed from valid_json, valid_fields, matches_schema"
    - "Confidence >= 0.7 accepts, >= 0.4 partial fallback, < 0.4 full fallback"
  artifacts:
    - path: "backend/app/llm/graph/nodes/llm_extraction.py"
      provides: "LLM JSON extraction node (single call)"
      exports: ["extract_structured"]
    - path: "backend/app/llm/graph/nodes/confidence_scoring.py"
      provides: "Confidence calculation with breakdown"
      exports: ["calculate_confidence"]
  key_links:
    - from: "backend/app/llm/graph/nodes/llm_extraction.py"
      to: "backend/app/llm/graph/nodes/confidence_scoring.py"
      via: "confidence_scoring.calculate_confidence(extracted)"
    - from: "backend/app/llm/graph/nodes/llm_extraction.py"
      to: "backend/app/llm/graph/state.py"
      via: "stores extracted filters in GraphState"
---

<objective>
Implement LLM structured extraction (single call) and confidence scoring system.

Purpose: Replace unstructured LLM SQL generation with structured JSON extraction + confidence-based routing.
Output: LLM extraction node + confidence scoring module
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-context-aware-hybrid/08-CONTEXT.md
@.planning/phases/08-context-aware-hybrid/08-RESEARCH.md
@.planning/phases/07-queryplan-compiler/07-03-SUMMARY.md
@backend/app/llm/graph/nodes/param_extractor.py
@backend/app/services/llm_provider.py

<interfaces>
<!-- Key interfaces from existing codebase -->

From backend/app/llm/providers/ (existing providers):
- LLMProvider.complete(prompt: str) -> str
- JSON output handling pattern

From backend/app/llm/graph/nodes/param_extractor.py:
- extract_params(state: GraphState) -> dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement LLM Structured Extraction</name>
  <files>backend/app/llm/graph/nodes/llm_extraction.py</files>
  <behavior>
    - extract_structured(question, context) returns {filters, sort, limit, follow_up_type}
    - Single LLM call per query
    - Strict JSON schema enforcement
    - Falls back to heuristic extraction on failure
  </behavior>
  <action>
Create llm_extraction.py with:
1. SYSTEM_PROMPT: Strict JSON schema instruction, no explanation
2. USER_PROMPT_TEMPLATE: Include question + semantic context + FieldRegistry info
3. extract_structured() node that:
   - Calls LLM with formatted prompt
   - Parses JSON response (handle repair_json for Ollama)
   - Validates fields against FieldRegistry
   - Returns dict with filters, sort, limit, follow_up_type

4. Fallback to heuristic extraction on:
   - JSON parse failure
   - Invalid fields
   - LLM provider unavailable

Create test file: backend/tests/test_llm_extraction.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.llm_extraction import extract_structured; print('llm_extraction imported')"</automated>
  </verify>
  <done>LLM extraction produces structured JSON output with field validation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Confidence Scoring</name>
  <files>backend/app/llm/graph/nodes/confidence_scoring.py</files>
  <behavior>
    - calculate_confidence(extracted_json, valid_fields, matches_schema) returns float + breakdown
    - valid_json: +0.3, valid_fields: +0.3, matches_schema: +0.4
    - >= 0.7: accept, >= 0.4: partial fallback, < 0.4: full ladder
  </behavior>
  <action>
Create confidence_scoring.py with:
1. calculate_confidence() function:
   - Check JSON validity → +0.3 if valid
   - Check field validity against FieldRegistry → +0.3 if all valid
   - Check schema match (filters have required keys) → +0.4 if matches
   
2. Decision thresholds:
   - >= 0.7: "accept" (use LLM extraction)
   - >= 0.4: "partial_fallback" (use some filters, heuristic for others)
   - < 0.4: "full_fallback" (trigger fallback ladder)

3. Return both confidence score and breakdown dict

Create test file: backend/tests/test_confidence_scoring.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.confidence_scoring import calculate_confidence; print('confidence_scoring imported')"</automated>
  </verify>
  <done>Confidence scoring correctly weights JSON validity, field validity, and schema match</done>
</task>

</tasks>

<verification>
- LLM extraction node follows single-call pattern
- JSON output strictly validated
- Confidence scoring implemented with correct thresholds
- Fallback to heuristic extraction on failure
</verification>

<success_criteria>
- [x] LLM extraction with strict JSON schema
- [x] Confidence scoring with breakdown
- [x] Decision routing based on thresholds
- [x] Tests stubbed for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-hybrid/08-02-SUMMARY.md`
</output>