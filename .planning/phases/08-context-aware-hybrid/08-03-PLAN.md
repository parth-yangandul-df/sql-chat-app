---
phase: 08-context-aware-hybrid
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - backend/app/llm/graph/nodes/deterministic_override.py
  - backend/app/llm/graph/nodes/conflict_resolution.py
autonomous: true
requirements: [HYB-10, HYB-11, HYB-12, HYB-13]
user_setup: []

must_haves:
  truths:
    - "Intent mismatch override: current_intent != last_intent → follow_up_type = new"
    - "Same field → REPLACE, Different field → ADD"
    - "Field validation: must exist in semantic layer + match intent schema"
  artifacts:
    - path: "backend/app/llm/graph/nodes/deterministic_override.py"
      provides: "Override rules that always win over LLM"
      exports: ["apply_overrides"]
    - path: "backend/app/llm/graph/nodes/conflict_resolution.py"
      provides: "Field-level conflict handling"
      exports: ["resolve_conflicts"]
  key_links:
    - from: "backend/app/llm/graph/nodes/deterministic_override.py"
      to: "backend/app/llm/graph/state.py"
      via: "reads last_intent, writes follow_up_type"
    - from: "backend/app/llm/graph/nodes/conflict_resolution.py"
      to: "backend/app/llm/graph/nodes/field_registry.py"
      via: "validates fields against registry"
---

<objective>
Implement deterministic override layer and conflict resolution for filter handling.

Purpose: Ensure deterministic rules always override LLM output, preventing LLM hallucinations from affecting SQL generation.
Output: Deterministic override node + conflict resolution module
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-context-aware-hybrid/08-CONTEXT.md
@.planning/phases/08-context-aware-hybrid/08-RESEARCH.md
@.planning/phases/07-queryplan-compiler/07-02-SUMMARY.md
@backend/app/llm/graph/nodes/field_registry.py

<interfaces>
<!-- Key interfaces from existing codebase -->

From backend/app/llm/graph/nodes/field_registry.py:
- FieldRegistry: validated field names for each domain
- validate_field(field_name, domain) -> bool

From backend/app/llm/graph/query_plan.py:
- FilterClause: field, op, values
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Deterministic Override Layer</name>
  <files>backend/app/llm/graph/nodes/deterministic_override.py</files>
  <behavior>
    - apply_overrides(extracted, state) returns overrides_applied + final_follow_up_type
    - Intent mismatch always sets follow_up_type = "new"
    - LLM-extracted follow_up_type overridden by deterministic rules
  </behavior>
  <action>
Create deterministic_override.py with:
1. apply_overrides() function that:
   - Checks if current_intent != last_intent → force follow_up_type = "new"
   - If same intent, keep LLM-extracted follow_up_type
   - Marks any overrides applied for observability

2. Key rule: Deterministic layer ALWAYS wins over LLM output

3. Integration: Called after LLM extraction + confidence scoring, before plan_updater

Create test file: backend/tests/test_deterministic_override.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.deterministic_override import apply_overrides; print('deterministic_override imported')"</automated>
  </verify>
  <done>Deterministic override correctly forces follow_up_type on intent mismatch</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Conflict Resolution</name>
  <files>backend/app/llm/graph/nodes/conflict_resolution.py</files>
  <behavior>
    - resolve_conflicts(new_filters, existing_filters) returns merged filters
    - Same field → REPLACE (remove existing, add new)
    - Different field → ADD (append new)
    - Validates fields against FieldRegistry
  </behavior>
  <action>
Create conflict_resolution.py with:
1. resolve_conflicts() function that:
   - Identifies field overlaps (same field name in new + existing)
   - For overlaps: REPLACE (remove old, add new)
   - For non-overlaps: ADD (append new)
   - Validates all fields against FieldRegistry
   
2. Field comparison: case-insensitive field name matching

3. Integration: Called in plan_updater to merge filters across turns

Create test file: backend/tests/test_conflict_resolution.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.conflict_resolution import resolve_conflicts; print('conflict_resolution imported')"</automated>
  </verify>
  <done>Conflict resolution correctly handles same-field replacement and different-field addition</done>
</task>

</tasks>

<verification>
- Deterministic override always wins over LLM
- Intent mismatch correctly forces follow_up_type = "new"
- Field conflicts resolved correctly (same = replace, different = add)
- Field validation against FieldRegistry
</verification>

<success_criteria>
- [x] Deterministic override layer implemented
- [x] Conflict resolution for field-level merging
- [x] Field validation integrated
- [x] Tests stubbed for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-hybrid/08-03-SUMMARY.md`
</output>