---
phase: 08-context-aware-hybrid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/llm/graph/state.py
  - backend/app/llm/graph/nodes/followup_detection.py
autonomous: true
requirements: [HYB-01, HYB-02, HYB-03, HYB-04, HYB-05]
user_setup: []

must_haves:
  truths:
    - "GraphState stores session_id, query embeddings, and follow_up_type"
    - "Follow-up detection correctly classifies refine/replace/new"
    - "Semantic similarity computed using cosine similarity"
  artifacts:
    - path: "backend/app/llm/graph/state.py"
      provides: "Extended GraphState with hybrid keys"
      min_lines: 50
    - path: "backend/app/llm/graph/nodes/followup_detection.py"
      provides: "Follow-up detection node with similarity calculation"
      exports: ["detect_followup_type"]
  key_links:
    - from: "backend/app/llm/graph/state.py"
      to: "backend/app/llm/graph/nodes/followup_detection.py"
      via: "import GraphState type"
---

<objective>
Extend GraphState with session/embedding fields and implement follow-up detection logic.

Purpose: Enable the hybrid system to track conversation context across turns and detect whether a query is a refinement, replacement, or new query.
Output: Extended GraphState TypedDict + followup_detection node
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-context-aware-hybrid/08-CONTEXT.md
@.planning/phases/08-context-aware-hybrid/08-RESEARCH.md
@.planning/phases/07-queryplan-compiler/07-RESEARCH.md
@.planning/phases/07-queryplan-compiler/07-01-SUMMARY.md
@backend/app/llm/graph/state.py
@backend/app/llm/graph/nodes/intent_classifier.py

<interfaces>
<!-- Key types and contracts from existing codebase -->

From backend/app/llm/graph/state.py:
```python
class GraphState(TypedDict):
    question: str
    connection_id: str
    ...
    query_plan: dict | None
    turn_context: dict | None
```

From backend/app/llm/graph/nodes/intent_classifier.py:
- classify_intent(state: GraphState) -> dict with domain, intent, confidence
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GraphState for Hybrid Mode</name>
  <files>backend/app/llm/graph/state.py</files>
  <action>
Extend GraphState TypedDict with the following new keys (following Phase 7 pattern of storing as dict, not Pydantic):
- session_id: str | None
- last_query: str | None
- last_query_embedding: list[float] | None
- current_query_embedding: list[float] | None
- semantic_similarity: float | None
- follow_up_type: Literal["refine", "replace", "new"] | None
- confidence_breakdown: dict | None

Add type imports from typing_extensions if needed.

Create test file: backend/tests/test_graph_state_extension.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.state import GraphState; print('GraphState extended successfully')"</automated>
  </verify>
  <done>GraphState includes all hybrid mode keys with correct type annotations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Follow-up Detection Node</name>
  <files>backend/app/llm/graph/nodes/followup_detection.py</files>
  <behavior>
    - detect_followup_type(current_embedding, last_embedding, current_intent, last_intent, filters_changed) returns "refine" | "replace" | "new"
    - semantic_similarity > 0.7 = "refine"
    - current_intent != last_intent = "new"
    - same_field_in_filters = "replace"
  </behavior>
  <action>
Create followup_detection.py with:
1. cosine_similarity(a, b) helper function
2. detect_followup_type() node function that:
   - Computes semantic similarity between current_query_embedding and last_query_embedding
   - Checks if intent changed
   - Checks if same field filters exist (for replace)
   - Returns follow_up_type + semantic_similarity

3. Integrate into graph.py: call after classify_intent, before extract_filters
   - Store current_query_embedding in state
   - Update last_query_embedding from previous turn

Create test file: backend/tests/test_followup_detection.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.followup_detection import detect_followup_type; print('followup_detection imported')"</automated>
  </verify>
  <done>Follow-up detection correctly classifies queries as refine/replace/new</done>
</task>

</tasks>

<verification>
- GraphState extended with all hybrid mode fields
- Follow-up detection runs after intent classification
- Semantic similarity computed correctly (cosine similarity)
- Integration with LangGraph pipeline complete
</verification>

<success_criteria>
- [x] GraphState includes session_id, last_query, embeddings, follow_up_type
- [x] Follow-up detection node implemented
- [x] Tests stubbed for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-hybrid/08-01-SUMMARY.md`
</output>