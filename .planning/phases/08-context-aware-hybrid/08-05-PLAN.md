---
phase: 08-context-aware-hybrid
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - backend/app/llm/graph/nodes/query_cache.py
  - backend/app/llm/graph/observability.py
autonomous: true
requirements: [HYB-20, HYB-21, HYB-22]
user_setup: []

must_haves:
  truths:
    - "Query cache key = hash(intent + filters + sort)"
    - "Cache reuses identical queries and repeated follow-ups"
    - "Structured logging captures query, intent, filters, follow_up_type, confidence, final_sql, fallback_used"
  artifacts:
    - path: "backend/app/llm/graph/nodes/query_cache.py"
      provides: "Query caching with hash-based key"
      exports: ["get_cached_result", "cache_result"]
    - path: "backend/app/llm/graph/observability.py"
      provides: "Structured logging for all queries"
      exports: ["log_query_context", "log_fallback_event"]
  key_links:
    - from: "backend/app/llm/graph/nodes/query_cache.py"
      to: "backend/app/llm/graph/nodes/filter_extractor.py"
      via: "cache key based on intent + filters"
    - from: "backend/app/llm/graph/observability.py"
      to: "backend/app/llm/graph/nodes/"
      via: "logs every node execution"
---

<objective>
Implement query caching and observability for the hybrid system.

Purpose: Optimize performance through caching and ensure full observability for debugging and monitoring.
Output: Query cache module + observability logging
</objective>

<execution_context>
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/ParthYangandul/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-context-aware-hybrid/08-CONTEXT.md
@.planning/phases/08-context-aware-hybrid/08-RESEARCH.md
@.planning/phases/07-queryplan-compiler/07-04-SUMMARY.md
@backend/app/core/logging.py

<interfaces>
<!-- Key interfaces from existing code -->

From backend/app/core/logging.py:
- Structured logging configuration
- Log levels and formatting

From backend/app/llm/graph/nodes/plan_updater.py:
- plan_updater stores filters in query_plan dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Query Caching</name>
  <files>backend/app/llm/graph/nodes/query_cache.py</files>
  <behavior>
    - get_cached_result(intent, filters, sort) returns cached result or None
    - cache_result(intent, filters, sort, result) stores in cache
    - Cache key = hash(intent + filters + sort) using hashlib
    - TTL: configurable (default 1 hour)
    - Max size: configurable (default 1000 entries)
  </behavior>
  <action>
Create query_cache.py with:
1. CacheKey class:
   - intent: str
   - filters: list[dict] (sorted for consistent hashing)
   - sort: list[dict] (sorted)
   
2. _make_key() function:
   - Serialize intent + filters + sort to JSON
   - hashlib.sha256 for key generation
   
3. get_cached_result(key) -> QueryResult | None:
   - Check if key exists and not expired
   - Return cached result or None
   
4. cache_result(key, result) -> None:
   - Store with timestamp
   - Evict oldest if max size exceeded (LRU)

5. Integration: Check cache after follow_up_detection, before LLM extraction

Create test file: backend/tests/test_query_caching.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.nodes.query_cache import get_cached_result, cache_result; print('query_cache imported')"</automated>
  </verify>
  <done>Query caching correctly generates hash keys and returns cached results</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Observability Logging</name>
  <files>backend/app/llm/graph/observability.py</files>
  <behavior>
    - log_query_context(query, intent, filters, follow_up_type, confidence, final_sql, fallback_used)
    - log_fallback_event(level, reason, extracted_filters)
    - All logging uses structured JSON format
  </behavior>
  <action>
Create observability.py with:
1. log_query_context() function:
   - Takes all required fields from PRD
   - Logs as structured JSON with appropriate level (INFO for normal, WARN for fallback)
   - Includes timestamp, session_id, execution_id

2. log_fallback_event() function:
   - Logs which level triggered (1-6)
   - Logs reason (e.g., "json_parse_failure", "invalid_fields", "low_confidence")
   - Logs extracted filters at each level

3. Integration:
   - Call log_query_context at end of pipeline
   - Call log_fallback_event at each fallback level trigger

Create test file: backend/tests/test_observability.py with stubs.
  </action>
  <verify>
    <automated>cd backend && python -c "from app.llm.graph.observability import log_query_context, log_fallback_event; print('observability imported')"</automated>
  </verify>
  <done>Observability logging captures all required query metadata</done>
</task>

</tasks>

<verification>
- Query caching with hash-based keys
- LRU eviction on max size
- Structured logging with all PRD fields
- Fallback events logged at each level
</verification>

<success_criteria>
- [x] Query caching implemented with hash keys
- [x] Observability logging with structured JSON
- [x] Integration with fallback events
- [x] Tests stubbed for Wave 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-context-aware-hybrid/08-05-SUMMARY.md`
</output>